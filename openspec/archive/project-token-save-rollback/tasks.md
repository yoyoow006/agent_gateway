# Tasks

- [ ] 1. TDD 红阶段
  - [x] 1.1 为 `workspace.New` 增加可注入保存函数；默认仍使用 `config.SaveLocal`。
  - [x] 1.2 增加测试：token 保存失败时不创建项目目录、agw.toml 或 .git。
  - [x] 1.3 增加测试：token 保存成功但项目目录创建失败时，回滚 local.toml 中本次 token 且删除项目目录。
  - [x] 1.4 增加测试：同名项目 token 已存在时失败且原 token 不变。
  - [x] 1.5 运行 workspace 测试并记录红证据。
- [ ] 2. 最小实现
  - [x] 2.1 调整 `New()`：检查同名 token、生成 token、先 SaveLocal、再创建项目目录/agw.toml/git init。
  - [x] 2.2 项目目录或模板写入失败时恢复原 local.toml 内容并删除本次项目目录。
  - [x] 2.3 保持 git init 失败仅警告的成功语义。
  - [x] 2.4 目标测试转绿并检查完整 diff。
- [ ] 3. 验证
  - [x] 3.1 运行 `go test ./internal/workspace -count=1`。
  - [x] 3.2 运行 `go test -race ./internal/workspace -count=1`。
  - [x] 3.3 运行 `go build ./... && go vet ./... && gofmt -l .`。
  - [x] 3.4 运行 `bash scripts/validate-workflow.sh --fast`。
  - [x] 3.5 运行 `openspec validate project-token-save-rollback --strict --no-interactive`。
- [ ] 4. 严格审查
  - [x] 4.1 Build 任务级审查回滚边界：仅删除本次新建目录，不误删既有路径。
  - [x] 4.2 Verify 规格符合性独立审查。
  - [x] 4.3 Verify 代码质量独立审查。
  - [x] 4.4 处置全部 Critical/Important 并记录未验证范围/残余风险。
- [ ] 5. 归档
  - [x] 5.1 合并 delta 到主规格。
  - [x] 5.2 更新 memory/kb 事实。
  - [x] 5.3 状态置为已归档并运行归档门禁。
